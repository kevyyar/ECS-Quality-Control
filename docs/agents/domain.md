# Domain docs

This repo uses a single-domain-doc layout:

- Project context file: `CONTEXT.md`
- Architectural decisions: `docs/adr/`

Consumer rules:
- `improve-codebase-architecture`, `diagnose`, `tdd`, and similar skills should read `CONTEXT.md` first.
- ADRs should be read from `docs/adr/` for historical architectural context.
