## Agent skills

### Issue tracker

GitHub Issues on `kevyyar/ecs-qc` via `gh issue`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default label mapping: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context setup (`CONTEXT.md` + `docs/adr/` at repo root). See `docs/agents/domain.md`.
